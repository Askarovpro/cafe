services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: b2b
      POSTGRES_PASSWORD: b2b
      POSTGRES_DB: b2b
    ports:
      - "5432:5432"
    volumes:
      - b2b-postgres:/var/lib/postgresql/data

volumes:
  b2b-postgres:
